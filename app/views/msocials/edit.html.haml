%h1.heading
  = "Editing msocial %s" % @msocial.id
  
- semantic_form_for(@msocial) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', msocials_path
