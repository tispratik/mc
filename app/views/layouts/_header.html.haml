#flash_messages
  - flash.each do |name, msg|
    = content_tag :div, msg, :class => "flash #{name}" if msg.is_a?(String)
        
- if @organization && !@organization.new_record?
  %h1
    = @organization
  
  .menu
    - [["Dashboard", "organizations", nil], ["Teams", "teams", :teams],].each do |link_name, link_controller, link_path|
      = link_to link_name, [@organization, link_path], :class => (controller_name == link_controller) ? "current" : nil

- elsif @user && !@user.new_record?
  %h1
    = @user
  
  .menu
    - [["Dashboard", "users", nil]].each do |link_name, link_controller, link_path|
      = link_to link_name, [User.find(@user.id), link_path], :class => (controller_name == link_controller) ? "current" : nil
%br