%h1.heading
  = "Editing team_user %s" % @team_user.id
  
- semantic_form_for(@team_user) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', team_users_path
